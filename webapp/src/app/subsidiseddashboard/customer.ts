export class Customer {
  name: string;
  usage: string;
  bookno: string;
  constructor(name: string, usage: string, bookno: string) {
    this.name = name;
    this.usage = usage;
    this.bookno = bookno;
  }
}
